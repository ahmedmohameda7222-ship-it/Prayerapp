name: Qibla pagehide lifecycle helper

on:
  push:
    branches:
      - feat/qibla-reliability-ux-hardening
    paths:
      - .github/workflows/qibla-pagehide-fix.yml
      - scripts/qibla-pagehide-fix.mjs

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/qibla-reliability-ux-hardening
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: node scripts/qibla-pagehide-fix.mjs
      - name: Commit bounded pagehide lifecycle fix
        run: |
          if git diff --quiet; then
            echo "No changes required"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add lib/use-qibla-controller.ts
          git commit -m "fix: pause qibla sensors on pagehide"
          git push origin HEAD:feat/qibla-reliability-ux-hardening
