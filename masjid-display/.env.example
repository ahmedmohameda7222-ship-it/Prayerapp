# Server-only TV project setting.
# Copy the real Prayerapp production origin from the root project's deployment
# settings into the deployed TV project's PRAYERAPP_ORIGIN value.
# Do not use a NEXT_PUBLIC_ variable and do not include a path/query/fragment.
PRAYERAPP_ORIGIN=
